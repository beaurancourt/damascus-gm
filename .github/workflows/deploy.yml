name: Deploy GM site

# The app itself lives in beaurancourt/damascus and builds two variants from
# one codebase. This repo exists to give the GM variant its own Pages site at
# /damascus-gm/, so it checks the app out rather than holding a copy of it.

on:
  # A push here rebuilds and republishes. The app repo's "Rebuild GM site"
  # workflow writes a marker commit naming the revision it wants built, so
  # changes to damascus/main reach this site without anyone coming here; the
  # manual trigger stays for rebuilding on demand.
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: pages
  cancel-in-progress: true

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the app
        uses: actions/checkout@v7
        with:
          repository: beaurancourt/damascus
          ref: damascus/main

      - uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        # Matching the app repo: `npm install`, not `npm ci`, so optional
        # native deps missing from a darwin-authored lock resolve on linux.
        run: npm install --no-audit --no-fund --prefer-offline

      - name: Build the GM variant
        run: npm run build:gm
        env:
          NODE_ENV: production

      - name: Configure Pages
        uses: actions/configure-pages@v6

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: dist-gm

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
